export type Skin = 'default' | 'standard' | 'danger' | 'warning' | 'urgent' | 'success';

export enum SKIN {
  default = 'default',
  standard = 'standard',
  danger = 'danger',
  warning = 'warning',
  urgent = 'urgent',
  success = 'success'
}

export const maxContentLength = 2;
