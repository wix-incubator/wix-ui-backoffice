export type Skin = 'standard' | 'success' | 'error';

export type Size = 'small' | 'medium' | 'large';

export enum SKIN {
  standard = 'standard',
  success = 'success',
  error = 'error'
}

export enum SIZE {
  small = 'small',
  medium = 'medium',
  large = 'large'
}
