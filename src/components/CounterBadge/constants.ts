export type Skin = 'general' | 'standard' | 'danger' | 'warning' | 'urgent' | 'success';

export enum SKIN {
  general = 'general',
  standard = 'standard',
  danger = 'danger',
  warning = 'warning',
  urgent = 'urgent',
  success = 'success'
}

export const maxContentLength = 2;
