export type Skin = 'default' | 'standard' | 'danger' | 'warning' | 'success';

export enum SKIN {
  default = 'default',
  standard = 'standard',
  danger = 'danger',
  warning = 'warning',
  success = 'success'
}

export const maxContentLength = 2;
