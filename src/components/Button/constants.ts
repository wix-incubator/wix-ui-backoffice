export enum Skin {
  standard = 'standard',
  light = 'light',
  destructive = 'destructive',
  premium = 'premium'
}

export enum Priority {
  primary = 'primary',
  secondary = 'secondary'
}

export enum Size {
  large = 'large',
  medium = 'medium',
  small = 'small',
  tiny = 'tiny'
}
