export enum Skin {
  standard = 'standard',
  white = 'white',
};

export enum Priority {
  primary = 'primary',
  secondary = 'secondary',
};

export enum Size {
  medium = 'medium',
  small = 'small',
}
