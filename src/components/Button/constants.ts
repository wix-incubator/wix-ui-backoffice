export enum Skin {
  standard = 'standard',
  white = 'white',
  destructive = 'destructive',
  premium = 'premium'
};

export enum Priority {
  primary = 'primary',
  secondary = 'secondary',
};

export enum Size {
  large = 'large',
  medium = 'medium',
  small = 'small',
  tiny = 'tiny',
}
